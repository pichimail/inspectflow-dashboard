'use client';
import {useEffect,useLayoutEffect,useMemo,useRef,useState} from 'react';
import Link from 'next/link';
import {usePathname,useRouter} from 'next/navigation';
import {PanelLeftClose,PanelLeftOpen,Search,MessageSquareText,Sun,Moon,RefreshCw,Mic,Volume2,X,Send,ChevronDown,Settings,FileInput,ShieldCheck,Clock,Download,Plus,Paperclip,Trash2,FileText} from 'lucide-react';
import {ROUTES,FOOTER_ROUTES,SAFE_AI_ROUTES} from '../lib/routes';
import {franchises,enquiries,inspections} from '../lib/data';

const welcome=[{role:'assistant',text:'Ask about clients, locations, inspectors, reports, workflow, or navigation. Attach documents or media to extract operational fields.'}];
function TipButton({label,side='bottom',children,className='',...props}){return <button type="button" className={`icon-button ${className}`} data-tip={label} data-tip-side={side} aria-label={label} {...props}>{children}</button>}
function isActive(path,href){return href==='/dashboard'?path==='/dashboard':path===href||path.startsWith(href+'/')}
const safeId=()=>typeof crypto!=='undefined'&&crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random().toString(36).slice(2)}`;

export default function ConsoleShell({children}){
 const path=usePathname(); const router=useRouter();
 const [collapsed,setCollapsed]=useState(false),[drawer,setDrawer]=useState(false),[theme,setTheme]=useState('light'),[command,setCommand]=useState(false),[query,setQuery]=useState(''),[agent,setAgent]=useState(false),[agentWidth,setAgentWidth]=useState(410),[messages,setMessages]=useState(welcome),[agentText,setAgentText]=useState(''),[busy,setBusy]=useState(false),[franchise,setFranchise]=useState('All Franchises'),[voiceBusy,setVoiceBusy]=useState(false),[voiceListening,setVoiceListening]=useState(false),[profileOpen,setProfileOpen]=useState(false),[openParents,setOpenParents]=useState({Enquiries:true,Inspections:true}),[threads,setThreads]=useState([]),[threadId,setThreadId]=useState(''),[historyOpen,setHistoryOpen]=useState(false),[attachments,setAttachments]=useState([]),[attachOpen,setAttachOpen]=useState(false),[historyReady,setHistoryReady]=useState(false);
 const drag=useRef(null),profileRef=useRef(null),historyRef=useRef(null),fileRef=useRef(null),attachRef=useRef(null);
 const currentRoute=useMemo(()=>[...ROUTES,...FOOTER_ROUTES].find(r=>isActive(path,r.href)),[path]);
 const current=currentRoute?.label||'InspectFlow';
 const context=useMemo(()=>({route:path,franchise,enquiries:enquiries.length,inspections:inspections.length,reportsPending:inspections.filter(x=>x.report!=='Submitted').length,unassigned:inspections.filter(x=>x.inspector==='No Inspector').length}),[path,franchise]);
 const groups=['Overview','Operations','Network'];

 useLayoutEffect(()=>{const initial=document.documentElement.dataset.theme==='dark'?'dark':'light';setTheme(initial);document.documentElement.style.colorScheme=initial},[]);
 useEffect(()=>{if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{})},[]);
 useEffect(()=>{const active=ROUTES.find(r=>isActive(path,r.href));if(active?.children)setOpenParents(p=>({...p,[active.label]:true}));setDrawer(false);setProfileOpen(false)},[path]);
 useEffect(()=>{const onKey=e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setCommand(v=>!v)}if(e.key==='Escape'){setCommand(false);setDrawer(false);setProfileOpen(false);setHistoryOpen(false);setAttachOpen(false)}};window.addEventListener('keydown',onKey);return()=>window.removeEventListener('keydown',onKey)},[]);
 useEffect(()=>{const onPointer=e=>{if(profileOpen&&!profileRef.current?.contains(e.target))setProfileOpen(false);if(historyOpen&&!historyRef.current?.contains(e.target))setHistoryOpen(false);if(attachOpen&&!attachRef.current?.contains(e.target))setAttachOpen(false)};document.addEventListener('pointerdown',onPointer);return()=>document.removeEventListener('pointerdown',onPointer)},[profileOpen,historyOpen,attachOpen]);
 useEffect(()=>{const open=e=>{setAgent(true);if(e.detail?.prompt)setAgentText(e.detail.prompt)};window.addEventListener('inspectflow:open-agent',open);return()=>window.removeEventListener('inspectflow:open-agent',open)},[]);
 useEffect(()=>{const move=e=>{if(!drag.current)return;setAgentWidth(Math.max(340,Math.min(620,window.innerWidth-e.clientX)))};const up=()=>drag.current=null;window.addEventListener('pointermove',move);window.addEventListener('pointerup',up);return()=>{window.removeEventListener('pointermove',move);window.removeEventListener('pointerup',up)}},[]);
 useEffect(()=>{try{const stored=JSON.parse(localStorage.getItem('inspectflow-agent-threads')||'[]');if(Array.isArray(stored)&&stored.length){setThreads(stored);setThreadId(stored[0].id);setMessages(Array.isArray(stored[0].messages)&&stored[0].messages.length?stored[0].messages:welcome)}else{const id=safeId();setThreadId(id);setThreads([{id,title:'New conversation',messages:welcome,updatedAt:Date.now()}])}}catch{const id=safeId();setThreadId(id);setThreads([{id,title:'New conversation',messages:welcome,updatedAt:Date.now()}])}finally{setHistoryReady(true)}},[]);
 useEffect(()=>{if(!historyReady||!threadId)return;setThreads(prev=>{const firstUser=messages.find(x=>x.role==='user')?.text?.trim();const title=firstUser?firstUser.slice(0,46):'New conversation';const next=[{id:threadId,title,messages,updatedAt:Date.now()},...prev.filter(t=>t.id!==threadId)].slice(0,24);try{localStorage.setItem('inspectflow-agent-threads',JSON.stringify(next))}catch{}return next})},[messages,threadId,historyReady]);

 const go=target=>{if(!SAFE_AI_ROUTES.has(target)&&!allowedDynamic(target))return;setCommand(false);setDrawer(false);setProfileOpen(false);setAgent(false);router.push(target)};
 const allowedDynamic=target=>['/inspectors/','/franchises/','/referrals/'].some(prefix=>String(target||'').startsWith(prefix));
 const searchItems=useMemo(()=>[...ROUTES.flatMap(r=>[{href:r.href,label:r.label},...(r.children||[]).map(([href,label])=>({href,label:`${r.label} · ${label}`}))]),...FOOTER_ROUTES].filter(x=>x.label.toLowerCase().includes(query.toLowerCase())),[query]);
 const applyTheme=next=>{const safe=next==='dark'?'dark':'light';document.documentElement.dataset.theme=safe;document.documentElement.style.colorScheme=safe;try{localStorage.setItem('inspectflow-theme',safe)}catch{}setTheme(safe)};
 const toggleTheme=()=>applyTheme(theme==='dark'?'light':'dark');

 function newChat(){const id=safeId();setThreadId(id);setMessages(welcome);setAgentText('');setAttachments([]);setHistoryOpen(false)}
 function openThread(thread){setThreadId(thread.id);setMessages(Array.isArray(thread.messages)&&thread.messages.length?thread.messages:welcome);setAgentText('');setAttachments([]);setHistoryOpen(false)}
 function removeThread(id){setThreads(prev=>{const next=prev.filter(t=>t.id!==id);try{localStorage.setItem('inspectflow-agent-threads',JSON.stringify(next))}catch{}return next});if(id===threadId)newChat()}
 function exportChat(){const body=messages.map(m=>`## ${m.role==='assistant'?'InspectFlow Assistant':'You'}\n\n${m.text}\n${m.attachments?.length?`\nAttachments: ${m.attachments.join(', ')}\n`:''}`).join('\n---\n\n');const blob=new Blob([`# InspectFlow Assistant Conversation\n\nExported ${new Date().toLocaleString()}\n\n${body}`],{type:'text/markdown'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`inspectflow-chat-${new Date().toISOString().slice(0,10)}.md`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
 async function processFiles(fileList){const incoming=[...fileList].slice(0,8);setAttachOpen(false);for(const file of incoming){const localId=safeId();setAttachments(a=>[...a,{id:localId,name:file.name,type:file.type||'file',size:file.size,status:'processing'}]);try{const form=new FormData();form.append('file',file);const res=await fetch('/api/ai/ingest',{method:'POST',body:form});const data=await res.json();if(!res.ok)throw new Error(data.error||'Attachment processing failed');setAttachments(a=>a.map(item=>item.id===localId?{...item,status:'ready',summary:data.summary||data.documentType||'Attachment processed',fields:data.fields||{},transcript:data.transcript||'',documentType:data.documentType||''}:item))}catch(e){setAttachments(a=>a.map(item=>item.id===localId?{...item,status:'error',summary:e.message||'Could not process file'}:item))}}if(fileRef.current)fileRef.current.value=''}

 async function ask(text=agentText){const clean=text.trim();if((!clean&&!attachments.length)||busy)return;const sentAttachments=attachments.filter(a=>a.status==='ready');const visibleText=clean||`Review ${sentAttachments.length} attached file${sentAttachments.length===1?'':'s'} and extract any InspectFlow data.`;setAgent(true);setMessages(m=>[...m,{role:'user',text:visibleText,attachments:sentAttachments.map(a=>a.name)}]);setAgentText('');setAttachments([]);setBusy(true);try{const res=await fetch('/api/ai',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:visibleText,context,attachments:sentAttachments.map(({name,type,summary,fields,transcript,documentType})=>({name,type,summary,fields,transcript,documentType}))})});const data=await res.json();setMessages(m=>[...m,{role:'assistant',text:data.message||'No response returned.',actions:data.actions||[],drafts:data.drafts||[],records:data.records||[]}])}catch{setMessages(m=>[...m,{role:'assistant',text:'The assistant service is temporarily unavailable. You can continue using InspectFlow.',actions:[],drafts:[]}])}finally{setBusy(false)}}
 async function speak(text){setVoiceBusy(true);try{const r=await fetch('/api/ai/tts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text})});if(!r.ok)throw new Error();const blob=await r.blob();const url=URL.createObjectURL(blob);const audio=new Audio(url);audio.onended=()=>{URL.revokeObjectURL(url);setVoiceBusy(false)};await audio.play()}catch{if('speechSynthesis'in window){speechSynthesis.cancel();speechSynthesis.speak(new SpeechSynthesisUtterance(text))}setVoiceBusy(false)}}
 async function startVoice(){
  if(voiceListening||busy)return;
  if(navigator.mediaDevices?.getUserMedia&&window.MediaRecorder){
   try{
    const stream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,channelCount:1}});
    const mime=['audio/webm;codecs=opus','audio/webm','audio/ogg;codecs=opus'].find(x=>MediaRecorder.isTypeSupported(x))||'';
    const recorder=new MediaRecorder(stream,mime?{mimeType:mime}:undefined);const chunks=[];setVoiceListening(true);
    recorder.ondataavailable=e=>{if(e.data?.size)chunks.push(e.data)};
    recorder.onstop=async()=>{try{const blob=new Blob(chunks,{type:recorder.mimeType||'audio/webm'});const bytes=new Uint8Array(await blob.arrayBuffer());let binary='';for(let i=0;i<bytes.length;i+=0x8000)binary+=String.fromCharCode(...bytes.subarray(i,i+0x8000));const data=btoa(binary);const format=(recorder.mimeType||'audio/webm').includes('ogg')?'ogg':'webm';const r=await fetch('/api/ai/stt',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({data,format})});const out=await r.json();if(!r.ok||!out.text)throw new Error(out.error||'Transcription failed');setAgentText(out.text);await ask(out.text)}catch(e){setMessages(m=>[...m,{role:'assistant',text:`Voice transcription failed: ${e.message||'Please try again.'}`}])}finally{stream.getTracks().forEach(t=>t.stop());setVoiceListening(false)}};
    recorder.start();setTimeout(()=>{if(recorder.state==='recording')recorder.stop()},7000);return;
   }catch{setVoiceListening(false)}
  }
  const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){setMessages(m=>[...m,{role:'assistant',text:'Microphone transcription is unavailable in this browser.'}]);return}const rec=new SR();rec.lang='en-AU';rec.interimResults=false;setVoiceListening(true);rec.onresult=e=>{const t=e.results[0][0].transcript;setAgentText(t);ask(t)};rec.onerror=()=>setVoiceListening(false);rec.onend=()=>setVoiceListening(false);rec.start()
 }

 return <div className={`app-viewport ${collapsed?'sidebar-collapsed':''}`}>
  <div className="app-frame">
  <a className="skip-link" href="#content">Skip to content</a>
  <aside className={`sidebar ${drawer?'mobile-open':''}`} aria-label="Primary navigation">
   <div className="brand"><span className="brand-mark">IF</span><span className="brand-copy"><strong>InspectFlow</strong><small>Operations</small></span></div>
   <nav className="sidebar-nav">
    {groups.map(section=>{const routes=ROUTES.filter(r=>r.section===section);return <div key={section} className="nav-group"><div className="nav-group-label">{section}</div><div className="nav-group-items">{routes.map(r=>{const Icon=r.icon;const active=isActive(path,r.href);const hasChildren=Boolean(r.children?.length);const expanded=Boolean(openParents[r.label]);return <div key={r.href} className={`nav-parent ${active?'active-parent':''}`}><div className="nav-parent-row"><Link href={r.href} className={`nav-link ${active?'active':''}`} data-collapse-tip={r.label}><Icon size={18}/><span>{r.label}</span>{r.badge&&<em>{r.badge}</em>}</Link>{!collapsed&&hasChildren&&<button type="button" className="nav-parent-toggle" aria-label={`Toggle ${r.label}`} aria-expanded={expanded} onClick={()=>setOpenParents(p=>({...p,[r.label]:!p[r.label]}))}><ChevronDown size={15}/></button>}</div>{!collapsed&&hasChildren&&expanded&&<div className="subnav">{r.children.map(([href,label])=><Link className={path===href?'active':''} key={href} href={href}>{label}</Link>)}</div>}</div>})}</div></div>})}
   </nav>
   <div className="sidebar-footer">{FOOTER_ROUTES.map(r=>{const Icon=r.icon;return <Link key={r.href} href={r.href} target={r.external?'_blank':undefined} className={`nav-link ${isActive(path,r.href)?'active':''}`} data-collapse-tip={r.label}><Icon size={18}/><span>{r.label}</span>{r.badge&&<em>{r.badge}</em>}</Link>})}</div>
  </aside>
  {drawer&&<button className="drawer-backdrop" onClick={()=>setDrawer(false)} aria-label="Close navigation"/>}

  <section className="workspace">
   <header className="topbar">
    <TipButton label={collapsed?'Expand navigation':'Collapse navigation'} side="bottom" onClick={()=>{if(window.innerWidth<900)setDrawer(true);else setCollapsed(v=>!v)}}>{collapsed?<PanelLeftOpen size={18}/>:<PanelLeftClose size={18}/>}</TipButton>
    <div className="breadcrumb"><span>InspectFlow</span><b>/</b><strong>{current}</strong></div>
    <button type="button" className="command-trigger" onClick={()=>setCommand(true)}><Search size={17}/><span>Search InspectFlow</span><kbd>⌘K</kbd></button>
    <div className="top-actions">
     <TipButton label="Refresh data" side="bottom" onClick={()=>router.refresh()}><RefreshCw size={17}/></TipButton>
     <TipButton label="Open InspectFlow assistant" side="bottom" onClick={()=>setAgent(true)}><MessageSquareText size={17}/></TipButton>
     <TipButton label={`Switch to ${theme==='dark'?'light':'dark'} mode`} side="bottom" onClick={toggleTheme}>{theme==='dark'?<Sun size={17}/>:<Moon size={17}/>}</TipButton>
     <label className="franchise-select"><span className="sr-only">Franchise scope</span><select value={franchise} onChange={e=>setFranchise(e.target.value)}>{franchises.map(f=><option key={f}>{f}</option>)}</select><ChevronDown size={14}/></label>
     <div className="profile-menu-wrap" ref={profileRef}><button type="button" className="profile-button" aria-haspopup="menu" aria-expanded={profileOpen} onClick={()=>setProfileOpen(v=>!v)}><span>AM</span><div><strong>Amy Morgan</strong><small>Head Office</small></div><ChevronDown size={14} aria-hidden="true"/></button>{profileOpen&&<div className="profile-menu" role="menu" aria-label="Account menu"><div className="profile-menu-meta"><strong>Amy Morgan</strong><small>Head Office · All Franchises</small></div><button role="menuitem" type="button" onClick={()=>{setProfileOpen(false);router.push('/fetch')}}><FileInput size={15}/><span>Document intake</span></button><button role="menuitem" type="button" onClick={()=>{setProfileOpen(false);router.push('/admin')}}><ShieldCheck size={15}/><span>Admin console</span></button><button role="menuitem" type="button" onClick={()=>{setProfileOpen(false);toggleTheme()}}><Settings size={15}/><span>{theme==='dark'?'Use light appearance':'Use dark appearance'}</span></button></div>}</div>
    </div>
   </header>
   <main id="content" className="page-content">{children}</main>
  </section>

  {command&&<div className="dialog-layer" role="presentation" onMouseDown={e=>{if(e.target===e.currentTarget)setCommand(false)}}><section className="command-dialog" role="dialog" aria-modal="true" aria-labelledby="command-title"><h2 id="command-title" className="sr-only">Search InspectFlow</h2><div className="command-search"><Search size={18}/><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search modules and actions"/><kbd>ESC</kbd></div><div className="command-results">{searchItems.length?searchItems.map(x=><button type="button" key={x.href} onClick={()=>go(x.href)}><span>{x.label}</span><small>Open</small></button>):<p className="command-empty">No matching modules or actions.</p>}</div></section></div>}

  {agent&&<aside className="agent-drawer agent-drawer-advanced" style={{width:agentWidth}} aria-label="InspectFlow assistant"><div className="agent-resizer" role="separator" aria-orientation="vertical" tabIndex={0} onPointerDown={e=>{drag.current=true;e.currentTarget.setPointerCapture?.(e.pointerId)}}/><header><div><strong>InspectFlow assistant</strong><small>Live records + multimodal intake</small></div><div className="agent-header-actions"><div className="agent-history-wrap" ref={historyRef}><TipButton label="Chat history" side="bottom" onClick={()=>setHistoryOpen(v=>!v)} aria-haspopup="menu" aria-expanded={historyOpen}><Clock size={16}/></TipButton>{historyOpen&&<div className="agent-history-menu" role="menu" aria-label="Assistant history"><header><b>Recent chats</b><button type="button" onClick={newChat}><Plus size={13}/>New</button></header>{threads.length?threads.map(t=><div className={`agent-history-row ${t.id===threadId?'active':''}`} key={t.id}><button type="button" role="menuitem" onClick={()=>openThread(t)}><b>{t.title}</b><small>{new Date(t.updatedAt).toLocaleString()}</small></button><button type="button" className="history-delete" onClick={()=>removeThread(t.id)} aria-label={`Delete ${t.title}`}><Trash2 size={13}/></button></div>):<p>No saved chats yet.</p>}</div>}</div><TipButton label="Export conversation" side="bottom" onClick={exportChat}><Download size={16}/></TipButton><TipButton label="New conversation" side="bottom" onClick={newChat}><Plus size={16}/></TipButton><TipButton label="Close assistant" side="bottom" onClick={()=>setAgent(false)}><X size={16}/></TipButton></div></header><div className="agent-context"><span><small>Module</small><b>{current}</b></span><span><small>Franchise</small><b>{franchise}</b></span><span><small>Open work</small><b>{context.reportsPending+context.inspections}</b></span></div><div className="agent-messages">{messages.map((m,i)=><article key={i} className={`agent-message ${m.role}`}><small>{m.role==='assistant'?'Assistant':'You'}</small><p>{m.text}</p>{m.attachments?.length>0&&<div className="agent-inline-files">{m.attachments.map(name=><span key={name}><Paperclip size={12}/>{name}</span>)}</div>}{m.drafts?.length>0&&<div className="agent-drafts" aria-label="Suggested field drafts"><header><b>Suggested field drafts</b><small>Review before applying to operational records</small></header>{m.drafts.map((d,index)=><div key={`${d.entity}-${d.field}-${index}`}><span><b>{d.entity}</b><small>{d.field}</small></span><strong>{String(d.value??'—')}</strong><em>{Math.round((d.confidence||0)*100)}%</em></div>)}<button type="button" onClick={()=>go('/fetch')}><FileText size={13}/>Review in Document Intake</button></div>}{m.role==='assistant'&&<div className="agent-message-actions"><button type="button" onClick={()=>speak(m.text)} disabled={voiceBusy}><Volume2 size={14}/>Speak</button>{(m.actions||[]).map(a=><button type="button" key={a.id||a.target} onClick={()=>go(a.target)}>{a.label}</button>)}</div>}</article>)}</div><div className="agent-suggestions">{['Find Michael Matthey','Show reports needing attention','Explain this page to a trainee','Open document intake'].map(s=><button type="button" key={s} onClick={()=>ask(s)}>{s}</button>)}</div><form className="agent-composer agent-composer-advanced" onSubmit={e=>{e.preventDefault();ask()}}>{attachments.length>0&&<div className="agent-attachment-tray" aria-live="polite">{attachments.map(file=><div className={`agent-attachment ${file.status}`} key={file.id}><Paperclip size={13}/><span><b>{file.name}</b><small>{file.status==='processing'?'Processing…':file.status==='error'?file.summary:file.documentType||'Ready'}</small></span><button type="button" onClick={()=>setAttachments(a=>a.filter(x=>x.id!==file.id))} aria-label={`Remove ${file.name}`}><X size={13}/></button></div>)}</div>}<textarea value={agentText} onChange={e=>setAgentText(e.target.value)} placeholder="Ask about a client, place, workflow, or attached file…"/><div><span>{voiceListening?'Listening…':busy?'Working…':attachments.some(a=>a.status==='processing')?'Processing attachment…':'Live page and record context included'}</span><div><div className="agent-attach-wrap" ref={attachRef}><input ref={fileRef} className="sr-only" type="file" multiple accept="image/*,audio/*,video/*,.pdf,.doc,.docx,.txt,.csv,.json,.xls,.xlsx,.ppt,.pptx" onChange={e=>processFiles(e.target.files||[])} /><TipButton label="Attach files" side="top" onClick={()=>setAttachOpen(v=>!v)} aria-haspopup="menu" aria-expanded={attachOpen}><Plus size={16}/></TipButton>{attachOpen&&<div className="agent-attach-menu" role="menu"><button type="button" role="menuitem" onClick={()=>fileRef.current?.click()}><Paperclip size={14}/><span><b>Attach media or document</b><small>Images, audio, video, PDF, Office, text and data files</small></span></button><button type="button" role="menuitem" onClick={()=>{setAttachOpen(false);go('/fetch')}}><FileInput size={14}/><span><b>Open Document Intake</b><small>Structured extraction workspace</small></span></button></div>}</div><TipButton label={voiceListening?'Listening…':'Voice command'} side="top" className={voiceListening?'recording':''} onClick={startVoice}><Mic size={16}/></TipButton><button type="submit" className="send-button" disabled={busy||(!agentText.trim()&&!attachments.some(a=>a.status==='ready'))}><Send size={15}/>Send</button></div></div></form></aside>}
  </div>
 </div>
}
